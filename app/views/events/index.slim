h1 Upcoming Events
p= link_to "iCalendar File URL", events_path(format: :ics)
- @events.each do |event|
    event
      .name = link_to event.title, event_path(event)
      .time
        - if event.recurring
          = event.pretty_recurring
          ' next on
        = event.pretty_time
      .description = event.short_description
